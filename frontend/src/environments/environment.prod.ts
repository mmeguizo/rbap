// Production environment configuration.
// This file is used during: ng build (production)
// The build system swaps this in place of environment.ts automatically.

export const environment = {
  production: true,

  // Replace with the real production API URL before deploying.
  apiUrl: 'https://your-production-backend.com/api/v1',
};
